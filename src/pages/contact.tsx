"use client";

import Head from "next/head";

export default function ContactPage() {

  return (
    <div className="relative flex h-screen w-screen items-center justify-center bg-bg2">
      <Head>
        <title>Contact DEV Community</title>
      </Head>
      Contact Page
    </div>
  );
}
